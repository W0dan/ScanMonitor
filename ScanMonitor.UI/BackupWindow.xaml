<Window x:Class="ScanMonitor.UI.BackupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ScanMonitor.UI"
        mc:Ignorable="d"
        WindowStartupLocation="CenterOwner"
        Loaded="Window_Loaded"
        Title="Backup in progress ..." Height="298.306" Width="683.145">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"></RowDefinition>
            <RowDefinition Height="1*"></RowDefinition>
        </Grid.RowDefinitions>

        <ListBox x:Name="BackupListBox" Grid.Row="0" Margin="20,20,20,10" />
        <ProgressBar x:Name="BackupProgress" Height="50" Grid.Row="1" Margin="20,10,20,20"/>

    </Grid>
</Window>
